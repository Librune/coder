import { Form, MetaData, SearchFn } from 'declare'

const metadata: MetaData = {
    name: '',
    uuid: '',
    baseUrl: '',
    userAgent: '',
    author: '',
    version: '',
}

const forms: Form[] = []

const actions = []

const search: SearchFn = (params) => {

}

const detail = ({ bid }) => {

}

const catalog = ({ bid }) => {
   
}

const chapter = ({ bid, cid }) => {
   
}
